import { GoBackBtnDirective } from './go-back-btn.directive';

describe('GoBackBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new GoBackBtnDirective();
    expect(directive).toBeTruthy();
  });
});
